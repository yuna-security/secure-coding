Exit code: 0
Wall time: 0.6 seconds
Output:
# Tiny Second-hand Shopping Platform — 개발 계획

> 작성: 2026-07-23, Claude (Fable 5) — v2 (Codex 리뷰 반영: 체크리스트 27개 정정, 송금 동시성/신고 무결성/Socket 인증 정정, 익명 조회·운영보안·측정가능 NFR 추가)
> 근거 자료: `과제요구사항.md`(전체 54줄), `secure-coding-slide.v2.pdf`(36쪽 전체 — 텍스트 추출 + 전 페이지 이미지 렌더링 확인), `강의전사문.txt`(전체 125줄), `docs/STARTER_SECURITY_AUDIT.ko.md`
> 목표: **과제 만점** — 기능 완전성 + 보안 약점 최소화 + 개발 전 과정이 추적 가능한 증적

---

## 1. 요구사항 확정 (슬라이드 22·25·35쪽 + 전사문 99~108행 기준)

### 1.1 필수 기능 7가지 (슬라이드 35쪽)

| ID | 요구사항 | 진행 방식 |
|----|----------|-----------|
| R1 | 사람들이 플랫폼에 가입할 수 있어야 함 | 25쪽 설계 항목 **모두 포함** |
| R2 | 상품들을 올리고 볼 수 있어야 함 | 25쪽 설계 항목 **모두 포함** |
| R3 | 플랫폼 사용자들끼리 소통이 가능해야 함 | 25쪽 설계 항목 **모두 포함** |
| R4 | 악성 유저나 상품을 차단해야 함 | 25쪽 설계 항목 **모두 포함** |
| R5 | 유저들 간의 송금이 가능해야 함 | **시스템 분석부터 새로 진행** |
| R6 | 상품을 검색할 수 있어야 함 | **시스템 분석부터 새로 진행** |
| R7 | 관리자가 플랫폼의 모든 요소를 관리할 수 있어야 함 | **시스템 분석부터 새로 진행** |

- 논리적으로 타당하면 요구사항 변경 가능, 추가는 무엇이든 가능. **변경 시 사유를 보고서에 기록**(전사문 106~107행).
- 비기능 요구사항(슬라이드 22쪽): 보안(최우선), 디자인, 서버 속도.

### 1.2 R1~R4의 세부 설계 명세 (슬라이드 25쪽 — 빠짐없이 구현해야 하는 목록)

- **R1 가입**: 로그인 페이지 / 회원가입 페이지 / 사용자 프로필 확인 / 마이페이지(소개글 **및 비밀번호** 업데이트) / 아이디 중복 금지 / 유저 정보 DB 관리
- **R2 상품**: 상품 등록 페이지 / 내가 등록한 상품 확인·관리 / 상품명·가격·사진 표시 / 등록 상품 누구나 조회 / 상품 정보 DB 관리 / 목록에서는 이름만, 클릭 시 상세 페이지
- **R3 소통**: 채팅 / 전체 유저 채팅 / 유저 간 1대1 채팅
- **R4 차단**: 불량 상품·사용자 신고 / 신고 사유 작성 필수 / 일정 횟수 이상 신고된 상품 차단 / 일정 횟수 이상 신고된 유저 휴면계정 전환

### 1.3 스타터 대비 미구현 기능 (슬라이드 29쪽 빨간 글씨 — 우리가 반드시 추가)

1. 사용자 조회 기능
2. 마이페이지의 **비밀번호 업데이트**
3. 등록된 상품 관리 기능(내 상품 수정·삭제)
4. 1대1 채팅 기능
5. 불량 상품 삭제 기능
6. 불량 유저 휴면 기능

### 1.4 채점·운영 관련 제약 (전사문에서 확인된 힌트)

- **멘토가 Codex로 README를 읽고 그대로 실행**함(104행) → README의 환경 설정·실행 절차는 클린 환경에서 무조건 재현되어야 함.
- **여러 유저가 채팅 가능해야 함** — 멘토는 작년에 서버 켜고 **핸드폰으로 접속해 채팅 테스트**(123행) → `0.0.0.0` 바인딩 + ngrok로 외부 접속 시나리오 검증.
- 리눅스(WSL/Ubuntu) 표준 환경(11행, 125행). 특정 OS가 필요하면 보고서에 명시.
- 언어·프레임워크 자유(108행)이나, 실습 기준 스택은 멘토 스타터(Flask)와 동일하게 유지.
- 송금 기능의 구현 수준은 "스스로 고민"(108행) → 실화폐가 아닌 **플랫폼 내 포인트/잔액 시스템**으로 정의하고, 그 논리적 근거를 보고서에 기록.
- 애자일로 돌리다 코드가 망가지지 않게, **워터폴처럼 설계를 먼저 확정**하고 단계적으로 구현(122~123행).
- 팀 과제 아님(개인), 표절 금지(123행).
- 제출: **2026-07-24(금) 23:59:59**, `[WHS][secure-coding][XX반]이름(전화번호뒷자리).pdf`(공백 없음), LMS 제출, GitHub public repo 링크 포함.

---

## 2. 기술 스택 및 아키텍처 결정

| 항목 | 결정 | 근거 |
|------|------|------|
| 언어/프레임워크 | Python 3 + Flask + Flask-SocketIO | 멘토 스타터와 동일, 강의 실습 기준 |
| DB | SQLite + **SQLAlchemy ORM** | 체크리스트 ORM 항목 충족. **ORM+파라미터 바인딩을 기본으로 사용하고, 동적 컬럼·정렬값은 허용목록으로 제한**(ORM 사용만으로 SQLi가 원천 차단되는 것은 아님 — raw SQL 조합·동적 정렬 필드 주입 가능) |
| 비밀번호 해시 | bcrypt (또는 Argon2) + 고유 salt | 체크리스트 "비밀번호 보안" |
| CSRF (HTTP) | Flask-WTF CSRFProtect — 모든 HTTP POST 폼 | 체크리스트 "CSRF 보호". **Socket.IO 이벤트는 CSRFProtect가 자동 보호하지 않음**(아래 채팅 항목에서 별도 처리) |
| 세션 | Flask 세션 쿠키 HttpOnly + SameSite=Lax + (HTTPS일 때) Secure, 만료 시간, 민감 작업 재인증 | 체크리스트 세션 쿠키·재인증 |
| 실시간 채팅 (Socket.IO) | Flask-SocketIO. **연결 시 Flask 세션 인증, 허용 Origin 명시, 이벤트마다 권한 재검사, 서버가 세션에서 발신자 ID 결정, DM Room 참여 권한 검사, 이벤트별 Rate Limiting** (CSRF 토큰이 아니라 이 통제들이 Socket 보안의 핵심) | 체크리스트 채팅 5항목 |
| 구조 | app factory + Blueprint(auth/product/chat/report/wallet/admin) + models + services | 유지보수성, 보고서 설계 설명 용이 |
| 환경 | WSL Ubuntu + miniconda(`environments.yaml`), ngrok로 외부 노출 테스트 | 슬라이드 16쪽 |

## 3. 데이터베이스 설계(초안)

- `user`: id, username(unique), password_hash, bio, role(user/admin), status(active/dormant), balance, created_at, failed_login_count, locked_until
- `product`: id, title, description, price(INTEGER, 원 단위), image_filename, seller_id(FK), status(active/blocked/deleted), created_at
- `report`: id, reporter_id(FK user), **reported_user_id(FK user, nullable), reported_product_id(FK product, nullable)**, reason, status(pending/auto_actioned/reviewed/resolved), resolution(nullable: upheld/reversed/dismissed), reviewed_by(FK user, nullable), reviewed_at(nullable), created_at
  - **다형성 `target_type/target_id` 대신 실제 FK 2개**로 대상 존재를 DB가 보장. `CHECK`로 둘 중 **정확히 하나만** non-null.
  - `UNIQUE(reporter_id, reported_user_id)` / `UNIQUE(reporter_id, reported_product_id)`로 대상별 사용자당 1회 신고.
- `chat_message`: id, room(global 또는 정규화된 dm 키), sender_id(FK), content, created_at — 발신자는 서버 세션에서 결정(클라이언트 값 신뢰 금지)
- `transfer` (예약어 회피를 위해 `transaction` 대신 사용): id, kind(transfer/grant), sender_id(FK, grant이면 NULL), receiver_id(FK), amount(INTEGER > 0), memo, idempotency_key, created_at — **불변 원장**. 일반 송금의 멱등 범위는 `(sender_id, idempotency_key)`이며, `user.balance`는 원장과 대조 가능한 materialized balance로만 유지한다.
- `audit_log`: id, actor_type(system/user/admin), actor_id(FK, system이면 NULL), action, target, detail, created_at — 관리자/자동 차단·휴면/시스템 지급 등 민감 조치 기록

**송금 동시성(SQLite 경쟁조건 방지)** — "단일 트랜잭션"만으로는 부족하므로:
1. 원자적 조건부 차감: `UPDATE user SET balance = balance - :amt WHERE id = :sender AND balance >= :amt`
2. **영향 행 수가 1일 때만** 입금(`balance = balance + :amt`)과 `transfer` 원장 기록을 같은 트랜잭션에서 수행. 0이면 잔액부족으로 롤백.
3. `(sender_id, idempotency_key)` 충돌 시 저장된 수신자·금액을 비교한다. 같으면 기존 성공 결과를 재응답하고, 다르면 `409 Conflict`로 거부한다.
4. 자기송금 금지, 양의 정수 금액 검증. **동일 요청 재전송·병렬 송금 테스트**를 P7에 포함.

> 상세 CHECK/UNIQUE, grant 지급, 신고 검토·복구 상태, 감사 actor 규칙은 `SYSTEM_DESIGN.ko.md` §5~6을 단일 구현 기준으로 삼는다.

슬라이드 28쪽의 최소 3테이블(사용자·상품·신고)을 포함하며 확장. 모든 테이블에 외래키·CHECK 제약·인덱스(검색용 `product.title` 인덱스) 명시.

## 4. 보안 요구사항 (공식 `secure_coding_checklist.csv` **27개 항목** 전면 반영)

> 정정: 슬라이드 31쪽 표는 행 번호 2~22까지만 보여 22개로 오해할 수 있으나, 실제 CSV에는 **"전체 시스템" 섹션 6개 항목**이 더 있어 총 27개다. (회원가입·프로필 7 + 상품 5 + 채팅 5 + 신고 4 + 전체 시스템 6 = 27)

**회원가입·프로필 (7)**: ①서버측 입력 검증(길이·허용 문자·형식, XSS 필터링) ②CSRF 토큰 ③bcrypt/Argon2+salt ④세션 쿠키 HttpOnly/Secure ⑤세션 만료·민감 작업 재인증 ⑥로그인 실패 횟수 제한(잠금/지연) ⑦오류 시 내부 정보(스택트레이스·DB 정보) 비노출
**상품 (5)**: ⑧폼 입력 서버측 검증(가격 숫자·범위) ⑨XSS 방어(이스케이프) ⑩로그인 사용자만 등록·수정·삭제 ⑪소유자 검증(IDOR 방지) ⑫데이터 무결성 검증
**채팅 (5)**: ⑬메시지 길이·내용 검증+XSS 이스케이프 ⑭Socket 연결 시 인증 확인 ⑮수신 메시지 서버측 검증 ⑯Rate Limiting(스팸 방지) ⑰운영 환경 WSS(TLS)
**신고 (4)**: ⑱target·reason 서버측 검증+XSS 방어 ⑲로그인 사용자만 접근 ⑳올바른 형식 저장+감사 로그 ㉑신고 남용 방지(중복·건수 제한·관리자 검토)
**전체 시스템 (6)**: ㉒ORM 및 파라미터 바인딩(+동적 컬럼·정렬 허용목록) ㉓DB 최소권한 원칙 ㉔보안 헤더(CSP, X-Frame-Options, X-Content-Type-Options) ㉕운영 HTTPS ㉖에러·예외 처리 시 민감정보 미노출 ㉗라이브러리·의존성 보안 패치 점검

**신고 남용 방지 세부(㉑ 구체화)**: 서로 다른 유효 사용자의 신고만 집계 · 자기 신고 금지 · 대상별 사용자당 1회 · 신규 계정 신고 가중치 또는 제한 · 임계치 도달 시 자동 숨김(상품)/휴면(유저) 후 **관리자 복구 가능** · 상태 변경은 감사 로그 기록. (집단 신고 악용 완화)

추가(스타터 약점 교정): SECRET_KEY 환경변수화, debug=False, 파일 업로드 검증(확장자·MIME·크기·난수 파일명), 자기 송금 금지, 송금 원자적 조건부 차감+idempotency(§3), 관리자 권한 분리.

## 4-1. 접근 제어 정책 (익명 조회 요구 반영 — 슬라이드 25쪽 "등록된 상품은 누구나 볼 수 있어야 함")

> 스타터는 `/dashboard`에 로그인을 요구하므로 이 구조를 그대로 확장하면 요구사항을 놓친다. 수용 기준(테스트로 검증):

- **비로그인 사용자도** 상품 목록·상세 조회 가능 (공개 목록/상세 라우트를 로그인 필요 대시보드와 분리)
- 등록·수정·삭제·신고·채팅·송금·마이페이지는 **로그인 필요**
- 차단(blocked)·삭제(deleted)된 상품, 휴면 유저의 상품은 일반 사용자에게 **비노출**, 관리자만 열람
- 관리자 라우트는 `role=admin`만 접근

## 4-2. 운영 보안 산출물 (전체 시스템 항목 구체화 — ㉒~㉗)

- 개발/테스트/운영 **설정 분리**(config 클래스 또는 env)
- 로컬 HTTP는 `SESSION_COOKIE_SECURE=False`, ngrok/운영 HTTPS는 `True` (환경별 토글)
- SQLite DB 파일·업로드 폴더 **OS 파일 권한** 최소화, 업로드는 실행 불가 위치
- **`.env.example` 제공, 비밀정보 미커밋**(.gitignore에 `.env`, `*.db`)
- 의존성 **정확한 버전 고정** + `pip-audit`(또는 `pip list --outdated`) 결과 증적
- 관리자 계정 생성용 **CLI 시드 명령**(안전한 초기 비밀번호 절차)
- DB 초기화·(필요 시)마이그레이션·시드 명령을 README에 명시
- 로그에 비밀번호·세션·민감 거래정보 **미기록**
- HTTPS/WSS 확인 방법(ngrok https 접속 스크린샷)

## 4-3. 측정 가능한 비기능 요구사항 (채점 가능 기준으로 구체화)

| 항목 | 기준(초안) |
|------|-----------|
| 상품 목록 | 페이지네이션 20개/페이지 |
| 검색 응답 | 로컬 기준 2초 이내 |
| 이미지 업로드 | 최대 5MB, 허용 확장자(jpg/png/webp) |
| 채팅 메시지 | 최대 500자 |
| 신고 사유 | 최대 1,000자 |
| 로그인 실패 | 5회 실패 시 일정 시간(예: 10분) 잠금/지연 |
| 채팅 Rate Limit | 사용자별 초당/분당 전송 횟수 제한(예: 5msg/5s) |
| 모바일 | 360px 화면에서 주요 기능 사용 가능 |

> 위 수치는 구현·테스트하며 조정하고, 변경 시 사유를 보고서에 기록.

## 5. 구현 로드맵 (커밋 단위로 증적을 남기며 진행)

| 단계 | 내용 | 산출물 |
|------|------|--------|
| P0 | 멘토 스타터 clone, 기준 커밋 기록, 실행 재현, **취약점 감사(before 증적)** | 감사 리포트, 스크린샷 |
| P1 | 요구사항 추적표(R1~R7→기능 ID), 유스케이스, ERD, 페이지 맵, 권한표, 위협 모델 | `docs/` 설계 문서 |
| P2 | 보안 공통 기반: app factory, ORM 모델, 인증(해시·세션·CSRF·로그인 제한), 보안 헤더, 에러 핸들러 | 코드 + 단위 테스트 |
| P3 | R1 유저 관리(가입/로그인/사용자 조회/마이페이지: 소개글·비밀번호 변경) | 코드 + 테스트 |
| P4 | R2 상품(등록/목록/상세/내 상품 수정·삭제/사진 업로드) + R6 검색 | 코드 + 테스트 |
| P5 | R3 채팅(전체 + 1:1, Socket 인증·검증·rate limit) | 코드 + 다중 브라우저 테스트 |
| P6 | R4 신고·차단(사유 필수, 임계치→상품 차단·유저 휴면, 중복 방지) | 코드 + 테스트 |
| P7 | R5 송금(포인트 잔액, 양의 정수·잔액·자기송금 검증, 원자적 트랜잭션, 이체 내역) | 코드 + 동시성 테스트 |
| P8 | R7 관리자(유저/상품/신고/거래 관리, 휴면 해제·차단 복구, 감사 로그 열람) | 코드 + 테스트 |
| P9 | 통합 하드닝: 체크리스트 **27항목** 전수 점검(after 증적), 침투 테스트(XSS/CSRF/SQLi/IDOR/업로드/스팸/송금 중복·병렬) | 체크리스트 CSV, 테스트 결과 |
| P10 | README(환경 설정·실행법, Codex가 읽고 바로 실행 가능하게), ngrok+핸드폰 다중 사용자 실사용 검증 | GitHub public repo |
| P11 | (별도 지시 후) 보고서 PDF: 요구사항 분석→설계→구현→체크리스트·테스트→유지보수, 보안 약점 before/after | 제출 PDF |

## 6. 테스트 전략 (슬라이드 30~32쪽)

1. **체크리스트**: 공식 체크리스트 27항목(`secure_coding_checklist.csv`) + 확장 항목으로 CSV 작성 → 항목별 확인 방법·결과 기록 (항목별 구현위치·검증·테스트 매핑은 `SYSTEM_DESIGN.ko.md` §11 부록)
2. **테스트 케이스**: pytest로 백엔드 기능별 정상/비정상 입력 케이스, 보안 케이스(XSS 페이로드, CSRF 토큰 누락, 타인 상품 수정 시도, 음수 송금, 잔액 초과, 중복 신고 등)
3. **실사용 테스트**: 서버 실행 후 두 개 이상 브라우저 + 핸드폰(ngrok)으로 회원가입→상품→채팅→신고→송금 전 과정, 스크린샷 증적 확보
4. **유지보수**: 발견 버그·불편 사항을 이슈 로그로 기록, 수정 커밋과 연결 (보고서의 유지보수 장에 사용)

## 7. 보고서 대비 증적 수집 원칙 (지금부터 습관화)

- 취약점은 **발견 시점의 코드/재현 화면(before)** 과 **수정 후(after)** 를 모두 보존
- 요구사항 ID → 설계 항목 → 코드 파일 → 테스트 ID → 결과가 연결되는 추적표 유지
- 요구사항을 변경·추가할 때마다 사유 한 줄 기록
