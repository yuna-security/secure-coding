# Mentor Starter Baseline

This repository preserves the user's existing Git history and imports the
mentor-provided starter as a separate assignment commit.

- Upstream repository: <https://github.com/ugonfor/secure-coding>
- Upstream branch: `main`
- Baseline commit:
  `f0dd4baac057f62315bb4850f05d18b7e60eb4be`
- Baseline commit date: 2025-03-22 17:35:44 +0900
- Local import commit: `846e8ec`

The pre-assignment state of this repository is tagged `pre-assignment`.
The upstream history was not force-merged or used to rewrite the user's
existing commits. Starter files were imported into the current history so that
subsequent security improvements remain easy to review as ordinary diffs.

The imported `README.md` was adapted only to use this repository's clone URL
and to identify its current status as the mentor starter baseline.
